Platform: Mobile (Cross-platform)
Framework: React Native
Primary Goal: An eyes-free assistant that uses speech activation to record user commands, capture a scene via camera, and return a descriptive audible analysis of the environment.
